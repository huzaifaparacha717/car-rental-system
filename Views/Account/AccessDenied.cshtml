@{
    ViewData["Title"] = "Access denied";
}

<div class="text-center my-5">
    <h1 class="display-6 text-danger"><i class="bi bi-slash-circle me-2"></i>Access denied</h1>
    <p class="text-muted">You do not have permission to view this page.</p>
    <div class="d-flex flex-wrap justify-content-center gap-2 mt-3">
        <a asp-controller="Car" asp-action="Index" class="btn btn-primary">Back to cars</a>
        <a asp-controller="Account" asp-action="AdminLogin" class="btn btn-outline-dark">Admin login</a>
        <a asp-controller="Account" asp-action="Login" class="btn btn-outline-secondary">Customer login</a>
    </div>
</div>
